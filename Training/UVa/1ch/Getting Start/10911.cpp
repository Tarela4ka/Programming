#include<bits/stdc++.h>

using namespace std;

const int INF = 1e9;
int n, m;

int main(){
    while(true){
        cin >> n;
        if (!n) break;
        string s;
        vector<pair<int, int>> cords(2n);
        for(int i = 0; i < 2n; i++) cin >> s >> cords[i].first >> cords[i].second;
        
    }
}
